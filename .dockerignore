# Ignore node_modules folders
node_modules
server/node_modules

# Ignore build output directory (it's created inside the container)
dist

# Ignore Git directory
.git

# Ignore Dockerfile and dockerignore itself
Dockerfile
.dockerignore

# Ignore environment files if they contain secrets
# (Secrets should be injected via Cloud Run, not baked into the image)
.env
.env.*
!.env.example

# Ignore IDE/OS specific files
.vscode
.idea
*.DS_Store

# Ignore logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log* 